package inf112.skeleton.model.gameobject.fixedobject;

import inf112.skeleton.model.gameobject.Transform;

public class Ground extends FixedObject {
    /**
     * Creates a new ground object with the specified transform.
     *
     * @param transform The fixed position and size of the Ground.
     */
    public Ground(Transform transform) {
        super(transform);
    }
}
